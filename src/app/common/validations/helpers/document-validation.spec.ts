import { DocumentValidation } from './document-validation';

describe('DocumentValidation', () => {
  it('should create an instance', () => {
    expect(new DocumentValidation()).toBeTruthy();
  });
});
