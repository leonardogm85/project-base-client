import { ZipCodeValidation } from './zip-code-validation';

describe('ZipCodeValidation', () => {
  it('should create an instance', () => {
    expect(new ZipCodeValidation()).toBeTruthy();
  });
});
