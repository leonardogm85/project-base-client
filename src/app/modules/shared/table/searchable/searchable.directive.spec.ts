import { SearchableDirective } from './searchable.directive';

describe('SearchableDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchableDirective();
    expect(directive).toBeTruthy();
  });
});
