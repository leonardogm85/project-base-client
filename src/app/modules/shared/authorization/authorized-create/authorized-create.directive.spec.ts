import { AuthorizedCreateDirective } from './authorized-create.directive';

describe('AuthorizedCreateDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorizedCreateDirective();
    expect(directive).toBeTruthy();
  });
});
