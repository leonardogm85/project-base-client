import { AuthorizedUpdateDirective } from './authorized-update.directive';

describe('AuthorizedUpdateDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorizedUpdateDirective();
    expect(directive).toBeTruthy();
  });
});
