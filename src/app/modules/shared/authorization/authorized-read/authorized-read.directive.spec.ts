import { AuthorizedReadDirective } from './authorized-read.directive';

describe('AuthorizedReadDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorizedReadDirective();
    expect(directive).toBeTruthy();
  });
});
