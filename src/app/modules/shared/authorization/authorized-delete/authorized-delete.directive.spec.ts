import { AuthorizedDeleteDirective } from './authorized-delete.directive';

describe('AuthorizedDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorizedDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
